{
  "name": "heroku-build",
  "version": "1.0.0",
  "description": "Script used to trigger react build during heroku deployment",
  "dependencies": {
    "bootstrap": "^4.5.2",
    "clsx": "^1.1.1",
    "react-bootstrap": "^1.3.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
